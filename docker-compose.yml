version: "3.9"

services:
  verifai:
    build: .
    container_name: verifai_app
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=production
    restart: unless-stopped
    volumes:
      # Persist model files between restarts
      - ./model:/app/model
